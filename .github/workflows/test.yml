name: test

on:
  workflow_dispatch:
    inputs:
      status:
    

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: |      
        statussuccess=238636
        statusfailure=d43442
        status=lightgrey
        status=status${{ github.event.inputs.status }}
        echo ${status}
